apiVersion: v1
kind: Service
metadata:
  name: {{ include "registration-service-chart.fullname" . }}
  labels:
    {{- include "registration-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "registration-service-chart.selectorLabels" . | nindent 4 }}
