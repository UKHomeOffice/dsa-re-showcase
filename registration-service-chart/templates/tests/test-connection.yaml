apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "registration-service-chart.fullname" . }}-test-connection"
  labels:
    {{- include "registration-service-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "registration-service-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
