kind: pipeline
type: kubernetes
name: build-test

platform:
  os: linux
  arch: amd64

steps:
  - name: helm
    image: quay.io/ukhomeofficedigital/helm:v3.5.4
    commands:
      - helm version

trigger:
  event:
  - push
