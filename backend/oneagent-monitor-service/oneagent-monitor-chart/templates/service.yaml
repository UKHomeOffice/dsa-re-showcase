apiVersion: v1
kind: Service
metadata:
  name: {{ include "oneagent-monitor-chart.fullname" . }}
  labels:
    {{- include "oneagent-monitor-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oneagent-monitor-chart.selectorLabels" . | nindent 4 }}